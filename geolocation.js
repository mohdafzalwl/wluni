import React from 'react'

class App extends React.Component {
window.navigator.geolocation.getCurrentPosition(
(positiion) => console.log(position),
(err)=> console.log(err)
);

render(){
return(
<div>hi</div>
)
}}

///examples
